apiVersion: v1
kind: Service
metadata:
  name: oficina-mailhog
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app: oficina-mailhog
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: ui
      port: 8025
      targetPort: 8025
