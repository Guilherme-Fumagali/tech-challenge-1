# Placeholders — NUNCA commitar valores reais. Gere o secret real via kubectl (ver k8s/README.md).
apiVersion: v1
kind: Secret
metadata:
  name: oficina-db-secret
  namespace: oficina
type: Opaque
stringData:
  POSTGRES_USER: REPLACE_ME
  POSTGRES_PASSWORD: REPLACE_ME
